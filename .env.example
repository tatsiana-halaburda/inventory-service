# Copy to .env (do not commit). Mac: unixodbc + msodbcsql18 for pyodbc. Run sql/*.sql in order before APIs.

DB_USERNAME=
DB_PASSWORD=
DB_SERVER=tcp:YOURSERVER.database.windows.net
DB_DATABASE=YOURDB

# Optional: overrides DB_* when set
# AZURE_SQL_CONNECTION_STRING=
# DB_ODBC_DRIVER=ODBC Driver 18 for SQL Server

# Practice 3 — Service Bus (listen): same queue name as Ordering send. Optional; listener skips if unset.
AZURE_SERVICEBUS_LISTEN_CONNECTION_STRING=
AZURE_SERVICEBUS_QUEUE_NAME=
# AZURE_SERVICEBUS_POLL_INTERVAL_SECONDS=5
